[
  {
    "name": "aggregate over one table - the recorded query rewritten with different spacing, keyword case, and a trailing semicolon",
    "score_mode": "binary",
    "outcome_classes": [
      "identical text scores 1.0",
      "whitespace/keyword-case/trailing-semicolon variant of the recorded query is accepted",
      "a different aggregate over the same column is rejected",
      "a different query over the same table is rejected"
    ],
    "expected": "SELECT avg(age) FROM Dogs",
    "input_data": {
      "question": "Compute the average age of all the dogs."
    },
    "metadata": {
      "row_id": "spider-dev-0377",
      "db_id": "dog_kennels",
      "split": "tuning",
      "scoring_path": "text-comparison"
    },
    "probes": {
      "good": "SELECT avg(age) FROM Dogs",
      "equivalent_good": "select   avg( age )   from   dogs ;",
      "partial": "SELECT age FROM Dogs",
      "bad": "SELECT count(*) FROM Dogs"
    }
  },
  {
    "name": "two columns with a quoted value - double quotes rewritten as single, and the case inside the value left alone",
    "score_mode": "binary",
    "outcome_classes": [
      "double-quoted and single-quoted spellings of the same literal are the same text",
      "comma padding and identifier case are folded",
      "case INSIDE a quoted value is a different value and is rejected",
      "a different filter value is rejected"
    ],
    "expected": "SELECT Population ,  LifeExpectancy FROM country WHERE Name  =  \"Brazil\"",
    "input_data": {
      "question": "What are the population and life expectancies in Brazil?"
    },
    "metadata": {
      "row_id": "spider-dev-0742",
      "db_id": "world_1",
      "split": "tuning",
      "scoring_path": "text-comparison"
    },
    "probes": {
      "good": "SELECT Population ,  LifeExpectancy FROM country WHERE Name  =  \"Brazil\"",
      "equivalent_good": "select population, lifeexpectancy from country where name = 'Brazil'",
      "partial": "SELECT Population , LifeExpectancy FROM country WHERE Name = 'brazil'",
      "bad": "SELECT Population , LifeExpectancy FROM country WHERE Name = 'Argentina'"
    }
  },
  {
    "name": "three aggregates in a list - comma spacing folded while the quoted country name keeps its case",
    "score_mode": "binary",
    "outcome_classes": [
      "a multi-item select list is compared after comma padding is folded",
      "keyword case is folded",
      "case inside the quoted filter value is preserved and rejected when it differs",
      "dropping two of the three aggregates is rejected"
    ],
    "expected": "SELECT avg(age) ,  min(age) ,  max(age) FROM singer WHERE country  =  'France'",
    "input_data": {
      "question": "What is the average, minimum, and maximum age of all singers from France?"
    },
    "metadata": {
      "row_id": "spider-dev-0405",
      "db_id": "concert_singer",
      "split": "tuning",
      "scoring_path": "text-comparison"
    },
    "probes": {
      "good": "SELECT avg(age) ,  min(age) ,  max(age) FROM singer WHERE country  =  'France'",
      "equivalent_good": "SELECT AVG(age), MIN(age), MAX(age) FROM singer WHERE country = 'France'",
      "partial": "SELECT avg(age), min(age), max(age) FROM singer WHERE country = 'france'",
      "bad": "SELECT avg(age) FROM singer"
    }
  }
]
