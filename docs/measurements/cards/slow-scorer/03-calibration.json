{
  "timed_out": true,
  "timeout_scope": "authored-calibration",
  "passed": false,
  "timeout_seconds": 5,
  "kind": "deterministic",
  "cases_requested": 4,
  "cases": [],
  "detail": "Evaluator calibration exceeded its 5-second budget. This does not by itself mean the evaluator is broken - slow and broken look identical from here. Do not silently re-run with a larger --timeout. Ask the user once, in one question, offering only what applies: wait, if the evaluator is normally this slow; take a named fix, if the cause is certain; score with a different judge model, or with a deterministic comparison - an exact or normalized match against the expected answer, no model call - where the task allows one; retry, since a provider call that has stalled looks the same from here; or build a new evaluation method together."
}
