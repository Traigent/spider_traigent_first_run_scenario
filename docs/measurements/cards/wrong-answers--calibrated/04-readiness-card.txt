$ python3 $GUIDE/readiness.py --preflight $EVIDENCE/02-preflight.json --agent-knobs $KNOBS/ready.json --agent-source-root $PROJECT --selected-agent $PROJECT/agent.py --selected-agent-callable run --agent-origin brought --calibration $EVIDENCE/03-calibration.json --evaluator-origin brought --evaluator-method normalized-exact --task-kind code-sql --color never --ascii
TRAIGENT OPTIMIZATION READINESS        90/100  WORKABLE

  AGENT       ####################  100/100  (agent source read; 1 of 5 checks measured)
    ? whether it ends, and on what                     not independently verified: one call per input, so it ends
    ? whether the answer's shape is pinned down        not independently verified: the answer's shape is constrained
    ? what the model is told, and shown                not independently verified: a prompt, no worked examples in it
    OK how many settings-combinations there are to try  static source verification established possible settings model, prompt_style, schema_context, temperature; this advisory read assumes the selected source does not rewrite its own functions or imports at runtime. It does not establish final request wiring. your space has 36 distinct configurations; this read carries no trial budget, so nothing here says how much of it a run would compare
    ? tools it declares, and can reach                 the document declares no tools, so tool wiring was not checked here.
    not covered by this pillar: whether your dataset and evaluation method are wired into the agent - this run builds that integration and verifies it against the installed SDK, so the opening read has nothing to measure

  DATASET     ##################--  91/100  (4 of 5 checks measured)
    OK range of difficulty           bands present: easy, hard, medium, very-hard
    ? repeated or dominant answers  not checked: whether one expected output dominates - the checks that did run found nothing, which is not the same as clean
    OK answers to score against      60/60 rows carry an expected output
    !! examples to compare on        48 to tune on / 12 held back; 48 examples - limited comparison set
    OK where the rows came from      60 collected of 60 rows; declared sources: real

  EVALUATION  #################---  83/100
    OK tried on answers already known right and wrong  4 calibration case(s); every authored check passed; this score read that result and did not take the measurement itself
    OK separates good answers from bad                 scores a right answer 1.00 above a wrong one, out of 1.00
    OK same answer every time                          deterministic scoring rule
    !! right kind of check for this output             normalized-exact is the wrong kind of check for code-sql output: it compares the answer with the expected text once case and spacing are normalised, so a right code-sql answer worded differently is still scored as wrong; normalized-exact fits closed-label, short-answer or routing output

  The detailed readiness report carries the source observations and settings-check guidance.

  No read of the expected answers this run is graded against has reached this score, so no score carries this comparison above WORKABLE. This hold is not a cap and does not stop the run: what it holds is the verdict, not the work. Nothing else here is capped, and this read is the only thing being asked of you. A read of the rows the run is graded on - each input beside its expected answer, up to 28 of them where the split is settled, or all of them if there are fewer, and a small sample of what you brought where it is not - is what lifts it.
  Local pre-run planning estimate, not a probability or measured optimization result.
Action: Review the expected answers that support this comparison.
exit=0
