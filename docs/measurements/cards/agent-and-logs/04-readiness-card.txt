$ python3 $GUIDE/readiness.py --preflight $EVIDENCE/02-preflight.json --agent-knobs $KNOBS/ready.json --agent-source-root $PROJECT --selected-agent $PROJECT/agent.py --selected-agent-callable run --agent-origin brought --task-kind code-sql --color never --ascii
TRAIGENT OPTIMIZATION READINESS        30/100  PARTIAL
Action: Review the expected results this run proposes for a representative set of examples.

  BLOCKER  30/100 PARTIAL is what your evidence supports, and that stands. Whether the
           paid run may start is a separate question: 2 things have to be cleared
           first, each marked FIX BEFORE PAID RUN below. Your assistant will explain
           and address these items with you, then recheck readiness before any paid
           comparison.

Confirmed absence is a measured structural finding. The missing component's quality has not been tested.

  FIX BEFORE PAID RUN Rows have inputs but no expected outputs, so there is nothing to score a configuration against.
  FIX BEFORE PAID RUN No evaluation method is connected, so no result can be trusted.

  AGENT       ####################  100/100  (agent source read; 1 of 5 checks measured)
    ? whether it ends, and on what                     not independently verified: one call per input, so it ends
    ? whether the answer's shape is pinned down        not independently verified: the answer's shape is constrained
    ? what the model is told, and shown                not independently verified: a prompt, no worked examples in it
    OK how many settings-combinations there are to try  static source verification established possible settings model, prompt_style, schema_context, temperature; this advisory read assumes the selected source does not rewrite its own functions or imports at runtime. It does not establish final request wiring. your space has 36 distinct configurations; this read carries no trial budget, so nothing here says how much of it a run would compare
    ? tools it declares, and can reach                 the document declares no tools, so tool wiring was not checked here.
    not covered by this pillar: whether your dataset and evaluation method are wired into the agent - this run builds that integration and verifies it against the installed SDK, so the opening read has nothing to measure

  DATASET     ####----------------  18/100  (3 of 5 checks measured)
    ? range of difficulty           no rows carry a difficulty tag - spread is unverified, not absent
    ? repeated or dominant answers  duplication was not checked
    !! answers to score against      40 rows, none with an expectation
    !! examples to compare on        no tuning set and held-out set, so the result would be measured on the same rows the search used; 40 rows, 0 scoreable; 0 comparable examples - a wiring check, not a score
    OK where the rows came from      40 collected of 40 rows; declared sources: real

  EVALUATION  --------------------  0/100  (1 of 4 checks measured)
    ? no evaluator
        !! tried on answers already known right and wrong
        ? separates good answers from bad
        ? same answer every time
        ? right kind of check for this output

  The detailed readiness report carries the source observations and settings-check guidance.

  Local pre-run planning estimate, not a probability or measured optimization result.
exit=0
