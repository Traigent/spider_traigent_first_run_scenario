$ python3 $GUIDE/readiness.py --preflight $EVIDENCE/02-preflight.json --agent-knobs $KNOBS/ready.json --agent-source-root $PROJECT --selected-agent $PROJECT/agent.py --selected-agent-callable run --agent-origin brought --task-kind code-sql --color never --ascii
TRAIGENT OPTIMIZATION READINESS        25/100  NOT READY

  BLOCKER  25/100 NOT READY is what your evidence supports, and that stands. Whether
           the paid run may start is a separate question: 2 things have to be cleared
           first, each marked FIX BEFORE PAID RUN below. Fix them, run this score
           again, and the paid comparison can start.

  AGENT       ##############------  70/100  (1 of 4 checks measured)
    ? whether it ends, and on what                     not independently verified; excluded from this score. Assistant observation: one call per input, so it ends (agent.py:387-406 run() is straight-line: one call_model call per input, no loop and no retry.)
    ? whether the answer's shape is pinned down        not independently verified; excluded from this score. Assistant observation: the answer's shape is constrained (agent.py:59 and agent.py:60-63 both instruct SQL only, and agent.py:321-346 strip_code_fence pins the returned answer to bare query text.)
    ? what the model is told, and shown                not independently verified; excluded from this score. Assistant observation: a prompt, no worked examples in it (agent.py:304-318 build_prompt assembles the exact text sent, and agent.py:378-383 passes it as the single user message; no worked examples ride with it.)
    !! how many settings-combinations there are to try  static source verification established possible settings model, prompt_style, schema_context; this advisory read assumes the selected source does not rewrite its own functions or imports at runtime. It does not establish final request wiring. your space has 18 distinct configurations; no trial budget was declared, so nothing here says how much of it this run compares; declaring `max_trials` is what lets this reach full credit - undeclared, it is held one step below; temperature: the cited executable source shows the declared options, but this deliberately narrow static read could not verify that changing this setting changes the request on the selected agent path; no source defect is inferred, and the enhanced run can settle this dimension
    ? tools it declares, and can reach                 the agent declares no tools, so tool wiring does not apply (agent.py declares no tools; the agent's only outbound call is the completion at agent.py:378.)
    not covered by this pillar: whether your dataset and evaluation method are wired into the agent - this run builds that integration and verifies it against the installed SDK, so the opening read has nothing to measure

  DATASET     ####----------------  18/100  (3 of 5 checks measured)
    ? range of difficulty           no rows carry a difficulty tag - spread is unverified, not absent
    ? repeated or dominant answers  duplication was not checked
    !! answers to score against      40 rows, none with an expectation
    !! examples to compare on        no tuning set and held-out set, so the result would be measured on the same rows the search used; 40 rows, 0 scoreable; 0 comparable examples - a wiring check, not a score
    OK where the rows came from      40 collected of 40 rows; declared sources: real

  EVALUATION  --------------------  0/100  (1 of 4 checks measured)
    ? no evaluator

  FIX BEFORE PAID RUN Rows have inputs but no expected outputs, so there is nothing to score a configuration against.
  FIX BEFORE PAID RUN No evaluation method is connected, so no result can be trusted.

  Local pre-run planning estimate, not a probability or measured optimization result.
exit=0
