$ python3 $GUIDE/readiness.py --preflight $EVIDENCE/02-preflight.json --agent-knobs $KNOBS/no-knobs.json --agent-source-root $PROJECT --selected-agent $PROJECT/agent.py --selected-agent-callable run --agent-origin brought --evaluator-origin brought --evaluator-method normalized-exact --task-kind code-sql --color never --ascii
TRAIGENT OPTIMIZATION READINESS        45/100  PARTIAL

  BLOCKER  45/100 PARTIAL is what your evidence supports, and that stands. Whether the
           paid run may start is a separate question: one thing has to be cleared
           first, marked FIX BEFORE PAID RUN below. Fix it, run this score again, and
           the paid comparison can start.

  AGENT       --------------------  0/100  (1 of 4 checks measured)
    ? whether it ends, and on what                     not independently verified; excluded from this score. Assistant observation: one call per input, so it ends (agent.py:109-116 run() is straight-line: one call_model call per input, no loop and no retry.)
    ? whether the answer's shape is pinned down        not independently verified; excluded from this score. Assistant observation: the answer's shape is constrained (agent.py:30 INSTRUCTION asks for the SQLite query only, and agent.py:43-69 strip_code_fence pins the returned answer to bare query text.)
    ? what the model is told, and shown                not independently verified; excluded from this score. Assistant observation: a prompt, no worked examples in it (agent.py:115 assembles the prompt from the schema and the fixed instruction at agent.py:30, and agent.py:100-105 sends it as the single user message; no worked examples ride with it.)
    !! how many settings-combinations there are to try  the agent was read and no varying setting was established - the read found no parameter the agent can vary
    ? tools it declares, and can reach                 the agent declares no tools, so tool wiring does not apply (agent.py declares no tools; the agent's only outbound call is the completion at agent.py:100.)
    not covered by this pillar: whether your dataset and evaluation method are wired into the agent - this run builds that integration and verifies it against the installed SDK, so the opening read has nothing to measure

  DATASET     ####################  98/100  (4 of 5 checks measured)
    OK range of difficulty           bands present: easy, hard, medium, very-hard
    ? repeated or dominant answers  not checked: whether one expected output dominates - the checks that did run found nothing, which is not the same as clean
    OK answers to score against      300/300 rows carry an expected output
    OK examples to compare on        240 to tune on / 60 held back; 240 examples - substantial comparison set
    OK where the rows came from      300 collected of 300 rows; declared sources: real

  EVALUATION  #######-------------  33/100  (2 of 4 checks measured)
    ? checked on known-good and known-bad  no calibration result was provided to this score
    ? separates good answers from bad      not yet measured how far apart it scores a right and a wrong answer
    OK same answer every time               deterministic scoring rule
    !! right kind of check for this output  normalized-exact is a poor ruler for code-sql output

  LIMITED TO 45 An evaluator method (normalized-exact) was declared, but no complete current-run calibration measured the connected evaluator, so this card cannot claim that it distinguishes answers. Complete calibration to establish that evidence.
  FIX BEFORE PAID RUN The agent was read and no setting it can vary was found, so every configuration a search compared would be identical.

  Local pre-run planning estimate, not a probability or measured optimization result.
exit=0
