$ python3 $GUIDE/readiness.py --preflight $EVIDENCE/02-preflight.json --agent-knobs $KNOBS/ready.json --agent-source-root $PROJECT --selected-agent $PROJECT/agent.py --selected-agent-callable run --agent-origin brought --evaluator-origin brought --evaluator-method execution --task-kind code-sql --color never --ascii
TRAIGENT OPTIMIZATION READINESS        85/100  WORKABLE
Action: Clarify the evaluator connection if that information is available.

  NOT CHECKED HERE An evaluator method (execution) was declared, and this run did not run the evaluator check on it, because running it here is outside the scope this guide permits. This run did not execute your original evaluator: doing so would run candidate code or open its configured target. This guide declines that direct route. So we do not know whether your evaluator works - not because you failed anything, but because this check is outside what this guide performs on your original file. The one step in this guide that could change that is the copied-actor route: a copy of your evaluator, calibrated against a copy of your database file this run makes or a read-only or duplicate target you supply, where the run can offer it. It is a limit of this run and not a judgement of your evaluator, and THIS does not stop your run. What proceeding means: during the paid run the MODEL writes the statements and your evaluator runs them against whatever it is configured to reach, many times over - generated statements, not yours. If it reaches an engine and that connection is read-only, the engine itself refuses a destructive statement, which closes that much - pass `--evaluator-connection read-only` and this card will say so. It closes nothing for an evaluator that runs candidate code outside a connection. Not answering is fine and changes nothing. Until some run measures this evaluator against answers already known to be right and wrong, no card can claim it grades correctly - so the evaluation pillar above reports which checks were measured, and this run cannot present as STRONG. Your score is not reduced for it.

  AGENT       ####################  100/100  (agent source read; 1 of 5 checks measured)
    ? whether it ends, and on what                     not independently verified: one call per input, so it ends
    ? whether the answer's shape is pinned down        not independently verified: the answer's shape is constrained
    ? what the model is told, and shown                not independently verified: a prompt, no worked examples in it
    OK how many settings-combinations there are to try  static source verification established possible settings model, prompt_style, schema_context, temperature; this advisory read assumes the selected source does not rewrite its own functions or imports at runtime. It does not establish final request wiring. your space has 36 distinct configurations; this read carries no trial budget, so nothing here says how much of it a run would compare
    ? tools it declares, and can reach                 the document declares no tools, so tool wiring was not checked here.
    not covered by this pillar: whether your dataset and evaluation method are wired into the agent - this run builds that integration and verifies it against the installed SDK, so the opening read has nothing to measure

  DATASET     ####################  98/100  (4 of 5 checks measured)
    OK range of difficulty           bands present: easy, hard, medium, very-hard
    ? repeated or dominant answers  not checked: whether one expected output dominates - the checks that did run found nothing, which is not the same as clean
    OK answers to score against      300/300 rows carry an expected output
    OK examples to compare on        240 to tune on / 60 held back; 240 examples - substantial comparison set
    OK where the rows came from      300 collected of 300 rows; declared sources: real

  EVALUATION  ############--------  59/100  (2 of 4 checks measured)
    ? tried on answers already known right and wrong  no calibration result was provided to this score; we wanted to try your evaluator on answers already known right and wrong, and nobody asked this run to make that check; no points are deducted for it - this run was not the one to make that measurement
    ? separates good answers from bad                 not measured here - calibration on the original evaluator is outside the scope this guide permits, and no points are deducted for it here
    OK same answer every time                          execution asks no model and samples nothing, so it is deterministic as a rule; what it reads is not, which is what this check deducts for
    !! right kind of check for this output             the evaluator file runs the answer, as execution declares: calls .execute() (line 159). This guide grades with an evaluator that does not run the answer, so a route that runs it is not credited as the right kind of check for this output

  The detailed readiness report carries the source observations and settings-check guidance.

  Some checks could not run (marked ? above), so this score rests on less evidence than a full one and stays at WORKABLE.
  Local pre-run planning estimate, not a probability or measured optimization result.
exit=0
