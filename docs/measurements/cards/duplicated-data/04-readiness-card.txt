$ python3 $GUIDE/readiness.py --preflight $EVIDENCE/02-preflight.json --agent-knobs $KNOBS/ready.json --agent-source-root $PROJECT --selected-agent $PROJECT/agent.py --selected-agent-callable run --agent-origin brought --evaluator-origin brought --evaluator-method normalized-exact --task-kind code-sql --color never --ascii
TRAIGENT OPTIMIZATION READINESS        35/100  PARTIAL

  BLOCKER  35/100 PARTIAL is what your evidence supports, and that stands. Whether the
           paid run may start is a separate question: one thing has to be cleared
           first, marked FIX BEFORE PAID RUN below. Your assistant will explain and
           address these items with you, then recheck readiness before any paid
           comparison.

  AGENT       --------------------  0/100  (agent source read; 1 of 5 checks measured)
    ? whether it ends, and on what                     not independently verified: one call per input, so it ends
    ? whether the answer's shape is pinned down        not independently verified: the answer's shape is constrained
    ? what the model is told, and shown                not independently verified: a prompt, no worked examples in it
    !! how many settings-combinations there are to try  no varying setting verified by this opening check
    ? tools it declares, and can reach                 the document declares no tools, so tool wiring was not checked here.
    not covered by this pillar: whether your dataset and evaluation method are wired into the agent - this run builds that integration and verifies it against the installed SDK, so the opening read has nothing to measure

  DATASET     #################---  86/100
    OK range of difficulty           bands present: easy, hard, medium, very-hard
    !! repeated or dominant answers  rows at least 70% similar to another row; not checked: whether one expected output dominates
    OK answers to score against      90/90 rows carry an expected output
    !! examples to compare on        73 to tune on / 17 held back, 25 of them repeat an input already counted, so 48 different example(s) are what a comparison can resolve; 48 examples - limited comparison set; the repeated-rows check fired, and this count subtracts exact repeats only
    OK where the rows came from      90 collected of 90 rows; declared sources: real

  EVALUATION  #######-------------  33/100  (2 of 4 checks measured)
    ? tried on answers already known right and wrong  no calibration result was provided to this score; it costs points until a complete calibration is measured
    ? separates good answers from bad                 not yet measured how far apart it scores a right and a wrong answer - this is measured at calibration, later in the run, and no points are deducted for it here
    OK same answer every time                          deterministic scoring rule
    !! right kind of check for this output             normalized-exact is the wrong kind of check for code-sql output: it compares the answer with the expected text once case and spacing are normalised, so a right code-sql answer worded differently is still scored as wrong; normalized-exact fits closed-label, short-answer or routing output

  FIX BEFORE PAID RUN 30 ids are used by more than one row, so a row cannot be named, excluded, or reviewed without ambiguity.
  WOULD LIMIT TO 45 An evaluator method (normalized-exact) was declared, but no complete current-run calibration measured the connected evaluator, so this card cannot claim that it distinguishes answers. Complete calibration to establish that evidence.
  WOULD LIMIT TO 45 The opening check has not established which settings change the request. This limits the planning score; it does not show that the agent has no settings. The request check before paid work is separate and does not raise this score.
  WOULD LIMIT TO 89 25 of the 73 rows this run can score on the tuning side repeat an input already counted, so this comparison resolves 48 different examples rather than 73. Nothing here is broken and the run can carry on, but every copy is a trial paid for that teaches what the first one already did. Continue on the examples that differ, or replace the repeats in your own file and this run reads it again.

  The detailed readiness report carries the source observations and settings-check guidance.

  REPEATED ROWS 25 of the 73 rows this run can score on the tuning side repeat an input already counted, so 48 different examples are what this comparison can resolve.
  A. Continue on the 48 examples that differ, and read the result as the smaller comparison it is.  (recommended: it keeps this run moving and buys comparisons your file does not hold yet)
  B. Replace the repeats in your own file, then say so and this run reads it again.
  Your file is read and never written to.

  Local pre-run planning estimate, not a probability or measured optimization result.
Action: Review a proposed dataset repair and validate its working copy.
exit=0
